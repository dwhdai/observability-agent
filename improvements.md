# Potential Improvements

* Dashboard panels should not move when user wants to zoom in on something
* Progressive disclosure of available data, services, metrics
* Dynamic panel: agent create ad-hoc dashboard panels at runtime, with custom SQL query generation and widget type to best match the user's query

